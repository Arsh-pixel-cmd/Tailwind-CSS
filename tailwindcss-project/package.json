{
  "name": "clone-of__x",
  "version": "1.0.0",
  "description": "This is clone of x(twitter) using tailwind css",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx @tailwindcss/cli -i ./src/input.css -o ./src/output.css --watch",
    "start": "npx live-server",
    "dev": "vite"
  },
  "author": "Arsh Mishra",
  "license": "MIT",
  "dependencies": {
    "nodemon": "^3.1.10",
    "vite": "^6.3.3"
  },
  "devDependencies": {
    "tailwindcss": "^4.1.5"
  }
}
